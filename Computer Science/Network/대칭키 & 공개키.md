## 대칭키(Symmetric Key)

### 개념

암호화와 복호화에 같은 암호키를 사용하는 알고리즘

서로 같은 비밀번호 키를 공유하고 그걸로 잠금과 해제를 함

### 장점

암호화 방식에 속도가 빠름

대용량 데이터 암호화에 적합

### 단점

키를 교환해야 하는 문제

사람이 증가할수록 키 관리가 어려워짐

확장성이 떨어짐

## 공개키(Public Key) / 비대칭키(Asymmetric Key)

### 개념

암호화와 복호화에 사용하는 암호키를 분리한 알고리즘

자물쇠(공개키/비대칭키)는 누구나 줄 수 있지만, 열쇠(개인키)로만 열 수 있음

### 장점

키분배가 필요하지 않음

보안성이 높음

### 단점

암호화 방식에 속도가 느림 

## 동작방식

### 대칭키

```
사용자 A -> 키 A -> 암호화 -> 암호문 -> 키 A -> 복호화 -> 원본 -> 사용자 B
```

- A와 B가 같은 키를 미리 공유해야만 통신이 가능함
- 키가 중간에 노출되면 통신 내용 전체가 위험함

⛔ 문제점: 키 교환이 어려움 (도청당하면 끝)

### 비대칭키 / 공개키

```
공개키 → 암호화
개인키 → 복호화
```

💡 비대칭키 / 공개키는 공개해도 상관없지만 **개인키는 절대 유출되면 안됨**

## 비대칭키(공개키)의 활용 - 전자서명

```
서명: 개인키로 암호화
검증: 공개키로 복호화
```

공개키로 복호화가 되면 그 메시지는 개인키를 가진 사람(본인)이 보낸 것이 맞다는 증거가 됨

⇒ 무결성과 송신자 인증

## 실제 서비스에서의 활용 - HTTPS / 웹

| 단계 | 사용 기술 | 이유 |
| --- | --- | --- |
| 초기 연결 시 | **비대칭키 (공개키)** | 안전하게 세션키(대칭키)를 공유하기 위해 |
| 데이터 전송 시 | **대칭키(AES)** | 빠르고 효율적으로 암호화하기 위해 |

서버의 공개키로 대칭키를 안전하게 전달한 후, 이후부터는 대칭키로 빠르게 암호화 통신을 진행

⇒ **대칭키 + 비대칭키를 혼합**해서 사용하는 **하이브리드 암호 시스템**

## 면접대비

### 대칭키와 비대칭키의 차이는 무엇인가?

대칭키는 하나의 키를 공유하지만, 비대칭키는 공개키/개인키 쌍으로 사용

대칭키는 빠르지만 안전하지 않고, 비대칭키는 안전하지만 느림

### 왜 실서비스에서는 비대칭키만 사용하지 않는가?

비대칭키는 느려서 대량 데이터 암호화에 비효율적

초기 키 교환만 비대칭, 이후 실데이터는 대칭키로 암호화

### HTTPS는 어떤 암호 방식을 사용하는가?

비대칭키로 세션키(AES)를 안전하게 교환하고, 이후 통신은 대칭키로 암호화하는 하이브리드 방식

### 전자서명은 어떤 키로 암호화하는가?

개인키로 암호화하고 공개키로 검증
